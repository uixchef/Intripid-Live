# lib/

Framework-agnostic helpers and third-party integration seams.

- `env.ts` — typed access to public environment variables.
- `mapbox/` — Mapbox GL integration (see its README for the required client boundary).
- `utils/` — small generic helpers.
